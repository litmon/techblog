box: wercker/default
build:
  steps:
    - arjen/hugo-build:
      version: 0.14
      theme: hugo-uno
      flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
      packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
      token: $GITHUB_TOKEN
      basedir: public

